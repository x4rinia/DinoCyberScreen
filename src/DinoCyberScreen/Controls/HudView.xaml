<UserControl x:Class="DinoCyberScreen.Controls.HudView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             Background="#000000">
    <Grid Background="#000000">
        <wv2:WebView2 x:Name="Browser" DefaultBackgroundColor="#000000" />
        <Image x:Name="PreviewImage" Visibility="Collapsed" Stretch="UniformToFill" HorizontalAlignment="Center" VerticalAlignment="Center" />
        <Border x:Name="Fallback" Visibility="Collapsed" Background="#000000">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="DINO CORE // WEB RENDERER OFFLINE" FontSize="22" Foreground="#26D9FF" HorizontalAlignment="Center" />
                <TextBlock x:Name="FallbackMessage" Margin="0,12,0,0" Foreground="#7895A4" FontSize="12" TextAlignment="Center" MaxWidth="720" TextWrapping="Wrap" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
