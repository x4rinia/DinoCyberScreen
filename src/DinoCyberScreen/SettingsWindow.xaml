<Window x:Class="DinoCyberScreen.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DinoCyberScreen // Configuration" Width="520" Height="500"
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen"
        Background="#000000">
    <Window.Resources>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#C9E7F1" />
            <Setter Property="FontFamily" Value="Cascadia Mono, Consolas" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Margin" Value="0,7" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="FontFamily" Value="Cascadia Mono, Consolas" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Height" Value="34" />
            <Setter Property="Margin" Value="0,5,0,14" />
            <Setter Property="Background" Value="#05090C" />
            <Setter Property="Foreground" Value="#D9F5FF" />
            <Setter Property="BorderBrush" Value="#237A91" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="9,3,38,3" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="DropDownToggle"
                                          Focusable="False"
                                          ClickMode="Press"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}"
                                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="ToggleBorder"
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition />
                                                    <ColumnDefinition Width="34" />
                                                </Grid.ColumnDefinitions>
                                                <Border Grid.Column="1" BorderBrush="#174B5D" BorderThickness="1,0,0,0" />
                                                <Path Grid.Column="1" Width="9" Height="5" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                      Data="M 0 0 L 4.5 4.5 L 9 0" Stroke="#26D9FF" StrokeThickness="1.4" />
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="ToggleBorder" Property="BorderBrush" Value="#26D9FF" />
                                                <Setter TargetName="ToggleBorder" Property="Background" Value="#09151A" />
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="ToggleBorder" Property="BorderBrush" Value="#3FFFC0" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="Left"
                                              VerticalAlignment="Center"
                                              IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              TextBlock.Foreground="{TemplateBinding Foreground}" />
                            <Popup x:Name="DropDownPopup"
                                   Placement="Bottom"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   PopupAnimation="Fade">
                                <Border MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                        MaxHeight="240"
                                        Background="#030608"
                                        BorderBrush="#26D9FF"
                                        BorderThickness="1">
                                    <ScrollViewer SnapsToDevicePixels="True" VerticalScrollBarVisibility="Auto">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="FontFamily" Value="Cascadia Mono, Consolas" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Background" Value="#05090C" />
            <Setter Property="Foreground" Value="#D9F5FF" />
            <Setter Property="Padding" Value="8,5" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBorder" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#12313B" />
                                <Setter Property="Foreground" Value="#FFFFFF" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#0B252D" />
                                <Setter Property="Foreground" Value="#3FFFC0" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter Property="Background" Value="#12313B" />
                    <Setter Property="Foreground" Value="#FFFFFF" />
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#0B252D" />
                    <Setter Property="Foreground" Value="#3FFFC0" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="GroupBox">
            <Setter Property="Foreground" Value="#26D9FF" />
            <Setter Property="BorderBrush" Value="#174B5D" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontFamily" Value="Cascadia Mono, Consolas" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Padding" Value="16,10" />
            <Setter Property="Margin" Value="0,0,0,16" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0" BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="1,1,1,0"
                                    Background="#05090C"
                                    Padding="10,6">
                                <ContentPresenter ContentSource="Header"
                                                  TextBlock.Foreground="{TemplateBinding Foreground}"
                                                  TextBlock.FontFamily="{TemplateBinding FontFamily}"
                                                  TextBlock.FontSize="{TemplateBinding FontSize}"
                                                  TextBlock.FontWeight="SemiBold" />
                            </Border>
                            <Border Grid.Row="1"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="1,0,1,1"
                                    Background="#020507"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter />
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="Cascadia Mono, Consolas" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Height" Value="34" />
            <Setter Property="Margin" Value="8,0,0,0" />
            <Setter Property="Foreground" Value="#A9CEDD" />
            <Setter Property="Background" Value="#05090C" />
            <Setter Property="BorderBrush" Value="#237A91" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="0">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              TextBlock.Foreground="{TemplateBinding Foreground}"
                                              TextBlock.FontFamily="{TemplateBinding FontFamily}"
                                              TextBlock.FontSize="{TemplateBinding FontSize}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#09151A" />
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#26D9FF" />
                                <Setter Property="Foreground" Value="#D9F5FF" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#0C1C24" />
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#3FFFC0" />
                                <Setter Property="Foreground" Value="#3FFFC0" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ScrollViewer">
            <Setter Property="Background" Value="Transparent" />
        </Style>
        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="#05090C" />
            <Setter Property="Foreground" Value="#174B5D" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="FontFamily" Value="Cascadia Mono, Consolas" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Height" Value="34" />
            <Setter Property="Margin" Value="0,5,0,14" />
            <Setter Property="Background" Value="#05090C" />
            <Setter Property="Foreground" Value="#D9F5FF" />
            <Setter Property="BorderBrush" Value="#237A91" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Cascadia Mono, Consolas" />
        </Style>
    </Window.Resources>
    <Grid Margin="24,18,24,18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="DINO CYBER SCREEN" FontSize="22" FontWeight="SemiBold" Foreground="#E8FAFF" />
            <TextBlock Text="SYSTEM CONFIGURATION // by X4RiNiA" Margin="0,4,0,0" Foreground="#26D9FF" FontSize="11" />
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <GroupBox Header="DATA MODULES">
                    <UniformGrid Columns="2">
                        <CheckBox x:Name="RealDataCheck" Content="Echte Systemdaten" />
                        <CheckBox x:Name="EventsCheck" Content="Visuelle Events" />
                    </UniformGrid>
                </GroupBox>

                <GroupBox Header="DISPLAY &amp; PERFORMANCE">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition Width="24" />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="MONITOR MODE" Foreground="#7895A4" FontSize="10" />
                            <ComboBox x:Name="MonitorCombo">
                                <ComboBoxItem Content="Alle Monitore" Tag="all" />
                                <ComboBoxItem Content="Hauptmonitor" Tag="primary" />
                            </ComboBox>
                            <TextBlock Text="ANIMATION QUALITY" Foreground="#7895A4" FontSize="10" />
                            <ComboBox x:Name="QualityCombo">
                                <ComboBoxItem Content="Low" />
                                <ComboBoxItem Content="Medium" />
                                <ComboBoxItem Content="High" />
                            </ComboBox>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="TARGET FPS" Foreground="#7895A4" FontSize="10" />
                            <ComboBox x:Name="FpsCombo">
                                <ComboBoxItem Content="30" />
                                <ComboBoxItem Content="60" />
                            </ComboBox>
                            <TextBlock Text="DINO TYP" Foreground="#7895A4" FontSize="10" />
                            <ComboBox x:Name="DinoCombo">
                                <ComboBoxItem Content="Ankylosaurus" Tag="ankylo" />
                                <ComboBoxItem Content="Triceratops" Tag="triceratops" />
                                <ComboBoxItem Content="Velociraptor" Tag="raptor" />
                                <ComboBoxItem Content="Stegosaurus" Tag="stego" />
                                <ComboBoxItem Content="Pterodactylus" Tag="ptero" />
                                <ComboBoxItem Content="Compsognathus" Tag="compy" />
                                <ComboBoxItem Content="Dino (Spezial)" Tag="special" />
                            </ComboBox>
                        </StackPanel>
                    </Grid>
                </GroupBox>

                <GroupBox Header="PERSONALIZATION &amp; COLOR PROFILE">
                    <StackPanel>
                        <TextBlock Text="SYSTEM NAME" Foreground="#7895A4" FontSize="10" />
                        <Grid Margin="0,5,0,12">
                            <TextBox x:Name="CustomNameText" MaxLength="8" />
                            <TextBlock Text="(Dino)" Foreground="#456070" FontSize="11" Margin="6,4,0,0"
                                       IsHitTestVisible="False" Padding="0">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Text, ElementName=CustomNameText}" Value="">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                        <TextBlock Text="THEME" Foreground="#7895A4" FontSize="10" />
                        <ComboBox x:Name="ThemeCombo" Margin="0,5,0,5">
                            <ComboBoxItem Content="Blue" />
                            <ComboBoxItem Content="Green" />
                            <ComboBoxItem Content="Red" />
                            <ComboBoxItem Content="White" />
                            <ComboBoxItem Content="Pink" />
                            <ComboBoxItem Content="Rainbow" />
                        </ComboBox>
                        <TextBlock Text="Rainbow ist fest mit Dino (Spezial) gekoppelt."
                                   Foreground="#557481" FontSize="9" Margin="0,2,0,0" />
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>

        <Grid Grid.Row="2" Margin="0,18,0,0">
            <TextBlock x:Name="PathText" VerticalAlignment="Center" Foreground="#45616E" FontSize="9" TextTrimming="CharacterEllipsis" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="ABBRECHEN" Width="112" Click="CancelClick" />
                <Button Content="SPEICHERN" Width="112" Foreground="#3FFFC0" BorderBrush="#2AAE88" Click="SaveClick" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
